# Prometheus  

En este **Readme** se muestra los archivos de configuracion que seran usados en el `compose.yaml`.

### Explicacion de `prometheus.yaml`.

Este archivo es el encargado de decirle a prometheus qué objetivo debe monitorizar, en el archivo actual, Prometheus se monitoriza a sí mismo.
