# Codigo python basico

Esta carpeta reune los recursos de codigo `python` usado en el workflow basico de `Basic-ci-python.yaml`.

## Qué hace este código

Este ejemplo de `python` muestra una estructura minima de aplicación y test.

- `App_python_test/app.py`: define la función `add(a, b)` para sumar dos valores.
- `Test_App_python/test_app.py`: verifica con `pytest` que la función devuelve el resultado esperado

Objetivo:

- entender una prueba básica en `python`
- ver cómo un pipeline valida código sencillo automaticamente




# Documentacion oficial 

Aqui encontraras enlaces a la documentacion oficial de `python`

- [🐍 Python](https://docs.python.org/es/3/)



> Este README.md se ira expandiendo segun las funciones Python que queramos.